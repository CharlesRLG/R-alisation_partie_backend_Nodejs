'use strict';

const index = require('./index.js');
require('path');
require('os');
require('fs-extra');
require('download');
require('consola');
require('decompress');
require('ora');
require('node-graceful-shutdown');

async function main() {
  const args = process.argv.splice(2);
  await index.startMongo({ args });
}
main().catch((err) => {
  console.error(err);
  process.exit(1);
});
