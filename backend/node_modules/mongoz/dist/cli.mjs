import { startMongo } from './index.mjs';
import 'path';
import 'os';
import 'fs-extra';
import 'download';
import 'consola';
import 'decompress';
import 'ora';
import 'node-graceful-shutdown';

async function main() {
  const args = process.argv.splice(2);
  await startMongo({ args });
}
main().catch((err) => {
  console.error(err);
  process.exit(1);
});
